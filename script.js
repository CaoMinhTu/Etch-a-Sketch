const GRID_DIMENSION = "512px"; // grid width and height

// đặt kích thước grid
document.getElementById('grid').style.height = GRID_DIMENSION;
document.getElementById('grid').style.width = GRID_DIMENSION;

let cellsPerSide = 16;

function createCells(cellsPerSide) {

}